<div class="container">
  <div class="row">
    <br><br><br>

    <p id="notice"><%= notice %></p>

    <div class="row align-items-md-stretch">
      <div class="col-md-6">
        <div class="h-100 p-5 bg-light border rounded-3">
          <h2>Un nouvel événement à partager ?</h2>
          <p>Sentez-vous libre de partager un événement, l'occasion de rencontrer de nouvelles personnes tout en se faisant un peu d'agent 🤑</p>
          <button class="btn btn-outline-secondary" type="button"><%= link_to 'Créer un nouvel événement', new_event_path %></button>
        </div>
      </div>
    </div>


    <h1><br>Liste des événements</h1>

    <table>

      <tbody>
        <div class="row align-items-md-stretch">
          <% @events.each do |event| %>
          <div class="col-sm-6 col-md-4 py-2">
            <div class="h-100 p-5 text-white bg-dark rounded-3">
              <p><%= event.start_date %>, durée : <%= event.duration %> minutes</td></p>
              <h2><%= event.title %></h2>
              <p><%= event.description %></p>
              <p>Prix : <%= event.price %> €, Lieu : <%= event.location %></p>
              <button class="btn btn-outline-light" type="button"><%= link_to 'Show', event %></button>
            </div>
          </div>
          <% end %>
        </div>
      </tbody>
    </table>
    <br>
  </div>
</div>